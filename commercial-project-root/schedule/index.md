# Schedule
milestones/ — one file per milestone (not a single master-schedule file —
  each milestone is its own record per the one-record-per-file rule).
lookahead-3week.md — rolling window, references task files by path.
dependencies.md — the task graph — query this to answer "why is X blocked."
