# Schema
Controlled vocabulary for the whole OKF instance: types.md, relationships.md, spatial-hierarchy.md.
Nothing outside this folder should introduce a new Type or relationship without registering it here first.
